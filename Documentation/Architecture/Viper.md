- When deadman switch not held, thrust to motors are zeroed

>[!Tip]
>You can turn on loopback in CAN interface to test without other devices
>`sudo ip link set can0 type can bitrate 1000000 loopback on`
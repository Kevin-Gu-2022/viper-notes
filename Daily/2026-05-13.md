## 📋 Tasks
- [ ]

## 📌 Notes
- Frames get discarded when Linux CAN buffer fills
	- No other  CAN frames get sent out
	- Only way I've found to get out of this is to cycle `can0` interface
- Only one TX error though
```bash
5: can0: <NOARP,UP,LOWER_UP,ECHO> mtu 16 qdisc pfifo_fast state UP mode DEFAULT group default qlen 64
    link/can 
    RX:  bytes packets errors dropped  missed   mcast           
         16125    2284      0       0       0       0 
    TX:  bytes packets errors dropped carrier collsns           
       3444340  573931      1       0       0       0 

```
- Stil times out after a while. Setting the txqueuelen buffer higher only kicks the can down the road
~~- Can happily ignore this issue though if I just set the `txqueuelen` buffer to 256, in line with Viper's internal software queue size for RX and TX~~


- Decreased the `NODE_LOOP_RATE` to 10us
- This rate controls how often the internal TX and RX CAN buffers are processed. Each loop, only one CAN message either side gets processed
- Increasing frequency should solve timeout issue

>[!Solution]
~~>Publish velocity only once every loop instead of one message per motor. I'm pretty sure bug is due to bursty traffic, though system should still need to handle it (yet to find a fix)~~

## 🌙 End of Day Review

### What to do tomorrow:
- [ ] 
